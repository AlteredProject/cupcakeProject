-- This script was generated by the ERD tool in pgAdmin 4.
-- Please log an issue at https://github.com/pgadmin-org/pgadmin4/issues/new/choose if you find any bugs, including reproduction steps.
BEGIN;


CREATE TABLE IF NOT EXISTS public.cupcake_bottoms
(
    bottom_id serial NOT NULL,
    name text COLLATE pg_catalog."default" NOT NULL,
    price numeric(10, 2) NOT NULL,
    CONSTRAINT cupcake_bottoms_pkey PRIMARY KEY (bottom_id)
);

CREATE TABLE IF NOT EXISTS public.cupcake_toppings
(
    topping_id serial NOT NULL,
    name text COLLATE pg_catalog."default" NOT NULL,
    price numeric(10, 2) NOT NULL,
    CONSTRAINT cupcake_toppings_pkey PRIMARY KEY (topping_id)
);

CREATE TABLE IF NOT EXISTS public.order_lines
(
    order_line_id bigserial NOT NULL,
    order_id bigserial NOT NULL,
    bottom_id serial NOT NULL,
    topping_id serial NOT NULL,
    quantity integer NOT NULL,
    unit_price numeric(10, 2) NOT NULL,
    CONSTRAINT order_lines_pkey PRIMARY KEY (order_line_id)
);

CREATE TABLE IF NOT EXISTS public.orders
(
    order_id bigserial NOT NULL,
    user_id bigserial NOT NULL,
    order_date date NOT NULL,
    total_price numeric(10, 2) NOT NULL,
    CONSTRAINT orders_pkey PRIMARY KEY (order_id)
);

CREATE TABLE IF NOT EXISTS public.users
(
    user_id bigserial NOT NULL,
    email text COLLATE pg_catalog."default" NOT NULL,
    password_hash text COLLATE pg_catalog."default" NOT NULL,
    credit_balance numeric(10, 2) DEFAULT 0,
    is_admin boolean NOT NULL DEFAULT false,
    CONSTRAINT users_pkey PRIMARY KEY (user_id),
    CONSTRAINT users_email_key UNIQUE (email)
);

ALTER TABLE IF EXISTS public.order_lines
    ADD CONSTRAINT bottom_id FOREIGN KEY (bottom_id)
    REFERENCES public.cupcake_bottoms (bottom_id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE NO ACTION;


ALTER TABLE IF EXISTS public.order_lines
    ADD CONSTRAINT order_id FOREIGN KEY (order_id)
    REFERENCES public.orders (order_id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE CASCADE;


ALTER TABLE IF EXISTS public.order_lines
    ADD CONSTRAINT topping_id FOREIGN KEY (topping_id)
    REFERENCES public.cupcake_toppings (topping_id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE NO ACTION;


ALTER TABLE IF EXISTS public.orders
    ADD CONSTRAINT user_id FOREIGN KEY (user_id)
    REFERENCES public.users (user_id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE NO ACTION;

END;